{
  "name": "kingsland-setup",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that serves the macOS setup script at https://kingsland.network/setup.sh",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "bash scripts/deploy.sh",
    "tail": "wrangler tail",
    "login": "wrangler login",
    "whoami": "wrangler whoami"
  },
  "devDependencies": {
    "wrangler": "^4.81.1"
  }
}
